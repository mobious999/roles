---
- name: Install package dependencies.
  package: name={{ item }} state=present
  with_items:
    - python
    - unzip

- name: Download the awscli bundle.
  get_url: url=https://awscli.amazonaws.com/awscli-exe-linux-x86_64.zip
  register: aws_cli_download_bundle

- name: Unarchive the installer.
  unarchive: src=/tmp/awscli-bundle.zip dest=/tmp copy=no creates=/tmp/awscli-bundle
  when: aws_cli_download_bundle.changed
  register: aws_cli_unarchive_installer

- name: Install awscli package.
  shell: python /tmp/awscli-bundle/install
  when: aws_cli_unarchive_installer.changed

- name: Create /etc/profile.d/awscli.sh to add awscli to PATH
  ansible.builtin.template:
    src: awscli.sh.j2
    dest: /etc/profile.d/awscli.sh
    owner: root
    group: root
    mode: '0755'
